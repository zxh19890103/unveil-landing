{
  "dependencies": {},
  "scripts": {
    "start": "bundle exec jekyll serve",
    "build": "JEKYLL_ENV=production jekyll build --config _config.yml,_config.prod.yml",
    "postbuild": "cd _quickdemo && npm run build"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "cssnano": "^6.0.5",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
}